package io.zefio.core.common.base;

/**
 * Enumeration defining the categorical types of plugins available in the core pipeline framework.
 */
public enum PluginType {
	ingress,
	interceptor,
	upstream,
	faultHandler
}
